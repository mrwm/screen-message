<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScreenMessage" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <item name="layer1">@color/darkLayer1</item>
        <item name="layer2">@color/darkLayer2</item>
        <item name="layer3">@color/darkLayer3</item>
        <item name="layer4">@color/darkLayer4</item>

        <item name="settings_background">@color/darkSettings</item>
    </style>
</resources>