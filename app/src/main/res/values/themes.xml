<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScreenMessage" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/black</item>
        <item name="layer1">@color/lightLayer1</item>
        <item name="layer2">@color/lightLayer2</item>
        <item name="layer3">@color/lightLayer3</item>
        <item name="layer4">@color/lightLayer4</item>

        <item name="settings_background">@color/lightSettings</item>
    </style>

    <style name="Theme.ScreenMessage" parent="Base.Theme.ScreenMessage" />
</resources>